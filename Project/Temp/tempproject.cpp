#include "tempproject.h"

TempProject::TempProject(const QString &empla, const QString &name, const std::vector<TempQuestion> &questions):
    empla(empla),
    name(name),
    questions(questions){}

